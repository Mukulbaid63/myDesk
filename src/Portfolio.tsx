import React from 'react';
import ProfileHeader from './components/ProfileHeader';
import { FooterWaveIcon } from './icons/FooterWave';

const Portfolio = () => {
    return (
        <div style={{fontFamily: 'Gentium Book Basic'}}>
        <ProfileHeader/>
        </div>
    );
};

export default Portfolio;